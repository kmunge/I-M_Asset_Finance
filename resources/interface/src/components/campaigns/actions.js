import axios from "axios";
import API from "../../endpoints";

axios.get(API.CAMPAIGNS)
.then();
// write a fucntion when called it fetches data and pipes it to a component
// repeat the same for other endpoints